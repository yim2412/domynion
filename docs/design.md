> ⚠ **2026-09-04 전면 대조.** 이 문서는 v0.1 자체 설계로 시작했고 2026-08-21 에
> 방향이 바뀌었다(목표: **핵심 규칙 구현 + 증강형 테크트리**).
> 그때 머리말만 붙이고 본문은 안 고쳐서, **1년치 낡은 사실이 살아 있는 조언처럼**
> 읽히고 있었다. 절마다 코드와 대조해 갈랐다:
>
> | 절 | 판정 |
> |---|---|
> | 1. 왜 이 조합인가 | ○ 유효 — 값이 아니라 의도다 |
> | 2. 규칙 | ✂ **한 줄도 안 맞아 전면 개정.** 공식을 다시 옮겨 적지 않고 **어디를 보면 되는지**만 적는다 |
> | 3. 증강 | ✂ 카드 표 정정 + *"미해결 할인 중첩"* 이 **다른 문제**였음을 실측(§5.117) |
> | 4. 파일 구조 | ✂ `core` 20개 중 6개만 적혀 있었다. 전면 갱신 |
> | 5. 먼저 겪은 함정 | ⚠ **살아 있는 것 넷 / 사라진 엔진 이야기 여덟**로 갈랐다 |
> | 6 · 6.5 | 📌 **스냅샷.** 옛 엔진의 측정이라 갱신하지 않는다 |
> | 7. 재개 지점 | ✂ **삭제.** 재개 지점은 [`implementation-log.md`](implementation-log.md) §7 **하나뿐**이다 |
>
> **구현 작업의 근거는 여전히 [`implementation-log.md`](implementation-log.md) 다.**
> 이 문서가 맡는 것은 **이 게임의 고유 계층**(증강)과 설계 의도뿐이다.

# Domynion 설계

실시간 영토 확장 전략 게임. **openfront.io 의 확장 방식**에 **증강 드래프트**를 얹는다.

작성 2026-08-20 · 전면 대조 2026-09-04.

---

## 1. 왜 이 조합인가

openfront 는 확장의 손맛이 좋지만 **모든 플레이어의 능력이 동일**하다. 그래서 후반이
"누가 더 부지런히 클릭했나"로 수렴하고, 판이 달라도 하는 일이 같다.

증강 드래프트를 얹으면 그 자리에 **빌드**가 생긴다. 정지 때마다 뽑힌 카드 중 하나를
고르고, 같은 카드를 다시 고르면 레벨이 오른다. 빌드를 미리 짜는 것이 아니라
**뽑힌 것들 사이에서 방향을 잡아 가는 것**이다.

정지 자체도 일한다 — 실시간이 끊김 없이 흐르기만 하면 오히려 단조로워진다.
정지가 판을 챕터로 쪼갠다.

---

## 2. 규칙

> ⚠ **2026-09-04 전면 개정.** 이 절은 **openfront 옛 엔진**(자체 대륙 생성)
> 기준이었고 **한 줄도 안 맞았다.** 공식 넷·지형 표·승리 조건이 전부 다른 값이었다.
> 아래에 옛 본문을 그대로 남긴다(그날의 판단 기록이라 지우지 않는다).
>
> **그리고 공식을 다시 베껴 적지 않는다.** 문서에 수식을 복사해 둔 것이 애초에
> 썩은 원인이다 — 코드가 바뀌어도 문서는 안 따라온다. `CLAUDE.md` 6번
> (*"상수는 한 곳에만"*)이 **문서에도 걸린다.** 여기는 **어디를 보면 되는지**만 적는다.

### 어디에 무엇이 있나

| 규칙 | 값 | 로직 |
|---|---|---|
| 병력 상한 · 성장 | `constants.py` (`MAX_TROOPS_*` · `TROOP_GROWTH_*`) | `state.py :: max_troops` · `troop_increase` |
| 한 칸 비용 · 확장 속도 | `constants.py` (`TERRAIN_MAG` · `TERRAIN_SPEED` · `ATTACKER_LOSS_*`) | `attack.py :: attack_logic` · `tiles_per_tick` |
| 지형 | `constants.py :: Terrain` · `TERRAIN_MAG` | `gamemap.py` |
| 승리 | `DOMINATION_TILE_PERCENT` · `MATCH_SECONDS` · `OVERTIME_*` | `engine.py :: domination_percent` · `_check_end` |

**전문은 `docs/implementation-log.md` 4절**에 참고 구현의 위치와 함께 있다. 그쪽이 대조
문서라 참고 구현과 나란히 놓여 있고, 값이 바뀌면 거기가 먼저 바뀐다.

⚠ **이 표를 쓰면서도 이름 둘을 틀렸다**(`troop_growth` · `resolve` — 둘 다 없는
이름이다). 적자마자 `grep` 으로 확인해서 잡았다. **가리키는 문서도 똑같이 썩는다** —
이름을 적었으면 그 자리에서 존재를 확인한다.

### 그래도 알아야 할 성질 셋 (값이 아니라 모양)

1. **자원은 병력 하나.** 영토가 상한을 정하고 병력이 상한에 수렴한다. 상한이
   **준선형**(타일^0.6)이고 **상수항이 크다** — 작은 지도에서는 상수항이 지배해
   영토를 넓혀도 상한이 거의 안 오른다.
2. **확장은 연속적이다.** 클릭한 칸이 아니라 **그 칸의 소유자 전체**가 대상이 되고,
   떼어 낸 부대가 국경을 따라 번지다 **병력이 떨어지는 지점에서 저절로 멈춘다.**
3. **지형은 셋뿐이다** — 평지 · 구릉 · 산악. **숲이 없다.** 바다는 통행 불가지만
   배는 물길만 이어지면 **어디든 간다**(사거리가 아니라 경로 탐색이다).
   옛 카드 둘(삼림 순찰대 · 항해술)이 갈 곳을 잃은 것이 이 둘 때문이다(§3).

### 승리

| 조건 | 값 | 비고 |
|---|---|---|
| 정복 | 마지막 생존 | |
| 지배 | 육지의 80% | ⚠ 분모는 **낙진을 뺀 땅**이다 |
| Overtime | 30분부터 문턱이 **분당 2%p** 내려가 **70분에 0** | 참고 구현의 기본은 꺼짐. **싱글이라 로비가 없어 우리가 켰다** |
| 시간 종료 | 170분 | ⚠ **도달 불가다** — Overtime 이 70분에 판을 끝낸다 |

---

<details>
<summary>옛 본문 (옛 엔진 · 2026-09-04 에 전부 틀린 것으로 확인) </summary>

### 자원은 병력 하나

영토가 병력 상한을 정하고, 병력이 상한에 로지스틱으로 수렴한다.
작은 나라는 빨리 차고 큰 나라는 오래 채운다 — 쌓아 두기만 하는 전략에 천장이 있다.

```
상한   = (250 + 영토 × 22) × 증강계수
성장률 = max(1.2, 상한 × 0.085 × (1 − 현재/상한)) × 증강계수   [초당]
```

### 확장은 연속적이다

```
한 칸 비용 = 2.6 × 지형방어 × (1 + 방어측_충전율 × 2.2) × 방어측_방어계수 × 공격측_비용계수
확장 속도  = min(90, 6 + 부대병력 × 0.055) × 공격측_속도계수    [초당 칸]
```

### 지형

| 지형 | 방어 |
|---|---|
| 평야 | 1.00 |
| 숲 | 1.20 |
| 구릉 | 1.45 |
| 산악 | 1.90 |
| 바다 | 통행 불가 (항해술 필요) |

### 승리

정복(마지막 생존) / 지배(육지 80%) / 시간 종료 15분 시 최대 영토.

</details>

---

## 3. 증강

### 원칙

> **증강은 새 규칙을 만들지 않고 기존 수치에 곱해지는 계수다.**

이 원칙이 무너지면 카드 한 장마다 예외 규칙이 하나씩 늘어 판정을 아무도 모르게 된다.

**2026-09-04 부터 예외는 없다. 열 장 전부 계수다.**

옛 원안의 예외는 항해술(닿는 범위를 바꾸는 카드)이었는데, 구현 과정에서
**대응물이 사라졌다** — 참고 구현의 배는 물길만 이어지면 어디든 간다(사거리가 아니라
경로 탐색이다). 같은 이유로 삼림 순찰대도 갈 곳이 없어졌다(참고 구현의 지형은
평지·구릉·산악 셋뿐, 숲이 없다). 둘 다 갈아끼웠다(§5.114).

### 드래프트

> ⚠ **2026-09-04 개정.** 아래 원안은 **openfront 구현 이전의 엔진**(판 ~15분)
> 기준이다. 지금 판은 실측 **33~37분**(§5.111)이라 주기가 안 맞고, 판 구성도
> 사람 1 + 나라 72 + 봇 400 으로 달라졌다. 개정된 결정 셋:
>
> | | 결정 | 왜 |
> |---|---|---|
> | **정지** | **멈추되 상한 10초.** 넘으면 무작위 선택 후 재개 | 스폰 페이즈(`SPAWN_PHASE_TURNS`)와 **같은 구조**다. 참고 구현도 안 고른 사람을 기다려 주지 않는다. 헤드리스 측정이 사람 없이도 그대로 흐른다 |
> | **대상** | **사람만** | 나라 72·봇 400 에 계수를 얹으면 매 tick 472명분 곱셈이 붙고, 봇의 성격(건물을 지우고 동맹을 다 받는다)과 빌드가 안 어울린다. **부수효과: §5.111 기준선이 계속 유효하다** — 그 측정은 사람 없이 돌린다 |
> | **주기** | **확정: 첫 정지 75초 → 이후 5분마다** | 잠정 180초로 뒀더니 **드래프트가 한 번도 안 열렸다** — 방치된 사람이 53~98초에 죽는다(§5.114). 원안 75초가 맞았다 |
> | **효과** | **확인됐다** — 켜면 생존판 영토 중앙이 **2.9배**(§5.116) | 같은 seed 12개를 켜고/끄고 24판. 생존율은 6/12 → 7/12 로 거의 안 움직였고 *살아남았을 때의 크기*가 갈렸다. **계수는 조정하지 않는다** |
>
> 원안(아래)은 **그날의 판단 기록이라 지우지 않는다.**

- 정지: 75초 → 이후 130초마다 = 한 판에 7회
- 매번 무작위 3장 → 하나 선택. 같은 카드를 다시 고르면 레벨 상승 (최대 Lv3)
- 레벨 배율 Lv1/2/3 = 1.0 / 1.7 / 2.3
- 최대 레벨인 카드는 후보에서 제외 (고를 수 없는 카드가 자리를 차지하면 선택지가 실질 2장)

### 카드 (10종)

| 카드 | 축 | Lv1 |
|---|---|---|
| 비옥한 땅 | `troops_cap_pct` | 상한 +18% |
| 징집령 | `troops_growth_pct` | 성장 +22% |
| 정예 병단 | `cost_vs_player_pct` | 적 영토 비용 −14% |
| 개척단 | `cost_vs_neutral_pct` | 중립 비용 −18% |
| 강행군 | `expand_speed_pct` | 확장 속도 +30% |
| 견고한 방벽 | `defense_pct` | 내 영토 방어 +22% |
| 산악병 | `cost_highland_pct` | 구릉·산악 비용 −32% |
| 교역로 | `trade_gold_pct` | 무역선 도착 골드 +25% |
| 초토화 | `defender_loss_pct` | 상대 병력 추가 손실 +35% |
| 상륙전 | `boat_loss_pct` | 상륙 퇴각 손실 −30% |

⚠ **갈 곳 없는 축을 남기면 카드가 조용히 아무 일도 안 한다** — 3장 중 하나가
꽝이 되고 그 사실이 화면 어디에도 안 나온다. `FIELDS` 의 모든 축이
`augments.py` **바깥 소스에 등장하는지** 재는 테스트를 뒀다.

같은 축에 여러 카드가 실리면 **더한다**(곱하지 않는다). 곱하면 카드가 쌓일수록 체감이
급격히 커져 후반이 한 명의 독주가 된다.

### 할인 중첩 — **2026-09-04 정정. 적어 둔 셋이 다 틀렸다**

증강 UI(보유 스트립)를 만들다 이 절을 다시 읽고 실측했다. 세 문장이 코드와
어긋나 있었고, 그래서 *"미해결"* 로 남아 있던 문제가 **애초에 다른 문제**였다.

| 적혀 있던 것 | 실제 |
|---|---|
| 정예 Lv3 + 산악병 Lv3 = **−106%** | **축이 다르다.** 합산은 축 안에서만 일어나고, **할인 카드는 축마다 정확히 하나씩**이라 어떤 축에서도 합이 안 생긴다. −106% 는 **일어날 수 없는 계산**이었다 |
| 하한 `max(0.2, …)` 가 막고 있다 | 하한은 **축마다** 걸리는데 `attack.py` 는 **곱**을 쓴다(`mult(cost_vs_player) × terrain × mult(defense)`). 실측 **0.179**, 중립·산악은 **0.155** — 하한 아래다. **막고 있지 않다** |
| 두 장으로 최대 할인, 그 뒤론 의미 없다 | 반대다. 축이 달라 **계속 곱해지고 포화가 없다.** 아래 *선택지 2(할인만 곱셈으로)* 가 **이미 그렇게 돌고 있다** |

**그래서 고치지 않는다.** 지켜야 하는 성질은 *"0.2 아래로 안 간다"* 가 아니라
*"0 이하로는 안 간다"* 이고, 양수의 곱은 양수이므로 **공짜 정복은 없다.**
할인이 세다고 보이면 그때 계수를 조정할 일이지 하한을 손볼 일이 아니다.

⚠ **왜 여태 안 드러났나** — 하한을 재는 테스트가
`Modifiers({"cost_vs_player_pct": -5.0})` 로 **손으로 만든 값**을 넣고 있었다.
카드로는 그 축에 한 장밖에 안 실려 **−5.0 은 만들 수 없는 값**이다. 테스트는
하한을 재고 있었지만 *"카드가 거기 닿는가"* 는 한 번도 안 봤다.
→ 테스트 둘을 추가했다: 어떤 카드 조합도 축 하한에 못 닿는다는 것,
그리고 **곱은 하한 아래로 내려가되 양수로 남는다**는 것.

옛 선택지 셋(원안, 그날의 판단 기록이라 지우지 않는다):
1. 레벨 배율(2.3)을 낮춘다 — 가장 간단하지만 고레벨의 보람이 줄어든다
2. 할인만 곱셈으로 — **축 간에는 이미 그렇다**
3. 체감 곡선: 합계 pct 를 `1 − exp(−x)` 류로 눌러 준다

**2026-08-21 실측(240판·4인):** 비용 하한(0.2)에 실제로 닿은 판이 **22/240 = 9.2%**.
드물지만 없는 일이 아니다. 다만 같은 측정에서 **더 큰 문제**가 나왔다 — 51%가 시간
종료이고 승자비중이 개척단 37.4% / 산악병 33.8%(기대 25%)로, 판이 "서로 못 뚫고
중립만 먹는" 모양이다. **할인을 먼저 깎으면 교착이 더 심해진다.** 그래서 순서를
바꾼다: 교착을 먼저 풀고, 그 위에서 할인 중첩을 다시 잰다.

---

## 4. 파일 구조

> ⚠ **2026-09-04 갱신.** 옛 표는 `core` 20개 중 **6개**, `ai` 10개 중 **2개**,
> `ui` 15개 중 **6개**만 적고 있었다. `gamemap.py` 설명도 *"대륙 생성(노이즈
> 높이맵)"* 이었는데 구현 후에는 **실제 지도 파일을 읽는다.**
> ✅/⬜ 표시도 뗐다 — 전부 있는 것이라 아무것도 안 가른다.

```
src/domynion/
  core/            순수 로직 — UI·AI·네트워크를 import 하지 않는다 (실측 확인)
    constants.py   밸런스 수치 단일 출처. 값 옆에 근거 위치를 적는다
    gamemap.py     지도 파일 적재 · 타일 · 인접 · 지형
    engine.py      tick · 승리 판정 · 증강 정지 · 영토 증분 카운트 (최대 파일)
    state.py       PlayerState + 증강이 반영된 파생 수치
    attack.py      공격 부대 · 프론티어 큐 · 연속 확장
    augments.py    카드 정의 · 레벨 · 계수 합산(Modifiers) · 드래프트
    buildings.py   건물 배치 자리 판정 · 초소 색인
    units.py       유닛 종류 · 레벨 · 보유량
    naval.py       무역선 · 전함 · 상륙선
    nukes.py       핵 · MIRV · SAM
    rail.py        철도망 · 역 그래프
    diplomacy.py   동맹 · 금수 · 표적
    relations.py   관계 수치
    emoji.py       이모지와 관계 변화
    events.py      이벤트 로그
    doomsday.py    둠스데이 클락
    rot.py         점진적 썩음
    enclave.py     둘러싸인 영토 흡수
    spawn.py       시작 위치 · 스폰 면역
  ai/              `core` 위에 얹는다. `ui` 를 참조하지 않는다 (실측 확인)
    nation.py      나라 AI 본체 (최대 파일)
    simple_ai.py   가벼운 AI + attach 배선
    nukes.py       핵 판단
    mirv.py        MIRV 판단
    structures.py  건설 판단
    placement.py   자리 고르기 값 함수
    alliance.py    동맹 판단 · 연장
    tribe.py       봇
    chatter.py     AI 가 먼저 거는 말
  ui/              `core` 위에 얹는다. `ai` 를 참조하지 않는다
    main_window.py 조립 · 실시간 타이머 · 배치
    map_widget.py  지도 그리기
    actions.py     클릭 → 명령
    hud.py         순위표 · 하단 바 · 면역 바
    radial.py      방사형 메뉴
    augment_dialog.py 증강 드래프트 창 + 보유 스트립
    overlays.py · frame.py · eventlog.py · endmodal.py · emojitable.py
    palette.py · rates.py · render.py · status.py · app.py
  cli/
    play.py        헤드리스 시뮬레이션 (밸런스 측정용)
    shot.py        스크린샷 모드
tools/             측정·검증 도구 — 아래 표
tests/             pytest. `conftest.py` 가 `tools/` 를 import 가능하게 한다
```

### `tools/`

| 도구 | 무엇을 재나 |
|---|---|
| `balance.py` | 기준선 — 핵 발사 · 생존 · 골드 · 종료 tick |
| `augment_ab.py` | 증강 켜고/끄고 A/B (생존율 + 짝 판정) |
| `gold_flow.py` | 골드가 어디로 나가나 |
| `profile_game.py` | 어느 계층이 시간을 먹나 |
| `mutate.py` | 변이 하네스 — 일부러 깨뜨려 테스트가 잡는지 |
| `verify_port.py` | 구현 대조 |
| `oracle.mts` | 참고 구현과 값 대조 |
| `_budget.py` | 병렬 작업이 CPU·RAM 여유 10% 를 남기게 |

**계층 규칙: `core` 는 `ui`·`ai`·`cli` 를 import 하지 않는다.** UI 와 AI 는
`core` 위에 나란히 얹고 서로를 참조하지 않는다. **2026-09-04 에 `grep` 으로
실제로 확인했다** — 규칙을 적어 두고 안 재면 어긋난 줄도 모른다.

---

## 5. 먼저 겪은 함정 (재구현 시 반복하지 말 것)

이 프로젝트는 한 번 갈아엎었고, 그 뒤 **openfront 로 통째로 구현**했다.
아래는 그때 실제로 당한 것들인데, **2026-09-04 에 하나씩 코드와 대조해**
아직 걸리는 것과 사라진 엔진 이야기를 갈랐다.

> ⚠ **가르기 전에는 전부 살아 있는 조언처럼 읽혔다.** fBm · 충전율 · 20Hz ·
> 서브셀 질감 — 코드에 **한 글자도 없는** 것들이 *"반복하지 말 것"* 표에
> 나란히 있었다. **틀린 조언은 없는 조언보다 나쁘다.**

### 아직 걸린다 (실측 확인)

| 함정 | 확인 |
|---|---|
| **`QWidget` 서브클래스는 스타일시트 background 를 자동으로 안 칠한다.** `WA_StyledBackground` 를 켜야 오버레이 HUD 배경이 나온다 | `ui/` 에서 **11번** 켠다 |
| **격자선을 지워야 영토가 덩어리로 보인다.** 소유자가 다른 변에만 굵은 경계선을 그려 그 선이 유일한 선이 되게 한다 | `map_widget.py` 머리말이 *"격자선 없음"* 을 규칙으로 들고 있다 |
| **확대할 때 `SmoothPixmapTransform` 을 끈다** | `map_widget.py :: paintEvent` |
| **지형 문양(나무·산 아이콘)을 또렷하게 그리면 격자가 되살아난다.** 색으로 구분한다 | `palette.py` 머리말 |

### 사라진 엔진 이야기 — **지금 코드에 대응물이 없다**

갈아엎기 전의 자체 대륙 생성 엔진(fBm 높이맵 · 충전율 비용 · 20Hz · 4인전
1,664칸) 시절의 것이다. **판단 기록이라 지우지 않지만, 조언으로 읽지 말 것.**

<details>
<summary>펼치기</summary>

- **노이즈를 타일 안에 가두면 안 된다** — fBm 자체가 없다. 지도는 **실제 지도 파일을 읽는다**
- **지형 레이어를 QPixmap 에 구워 둔다**(캐시 5.8ms vs 90.5ms, 15.6배) — 서브셀 질감이 없어졌다
- **타일 사각형을 정수 픽셀에 맞출 것** — 그리기 경로가 통째로 바뀌었다
- **라벨 폰트를 영토 덩어리 실제 폭에 맞춘다** — 실제 폭 계산으로 대체됐다(`frame.py`). §5.97 에서 **우리가 쓰던 무게중심 방식이 틀렸다**고 밝혀졌다
- **방어 비용에 '타일당 병력'(절대값)을 쓰면 교착된다 → 충전율(0~1)로 정규화** — 참고 구현은 **손실에만** 쓰고 비용에는 안 쓴다. `attack.py` 주석이 그 자리를 적어 뒀다
- **시작 위치는 전원 같은 대륙에** — 스폰 규칙(`spawn.py`)으로 대체
- **지형 임계값은 절대 높이가 아니라 백분위로** — 지형이 지도 파일에 이미 들어 있다
- **`tile_count` 전수 순회 금지**(1,664칸 × 4명 × 20Hz) — 증분 카운트는 **지금도 그렇게 돈다**. 다만 숫자가 다르다: 지금은 **2000×1000 지도 · 472명 · 10Hz**
- **눈덩이가 빠르다**(40판 전부 지배, 중앙값 73초) — 지금 판은 **33~37분**이다

</details>

---

## 6. 이번에 당한 것 (2026-08-21) — **스냅샷. 갱신하지 않는다**

> ⚠ **이 절과 §6.5 는 openfront 옛 엔진**(4인전 · 1,664칸 · 충전율 비용 ·
> `NEUTRAL_BIAS` 같은 AI 상수)의 측정 기록이다. 그 엔진은 없어졌고 여기 나오는
> 상수는 **코드에 하나도 없다.** 그날의 판단 기록이라 남기지만, **지금 판에 대한
> 사실로 읽으면 안 된다.** 특히 §6.5 끝의 *"남은 구조적 결함 — 충전율이 포화한다"*
> 는 **더 이상 열려 있는 문제가 아니다** — 그 규칙 자체가 사라졌다.

- **테스트가 아무것도 재고 있지 않았다.** `test_blocked_tile_goes_back_to_front_of_queue`
  를 `dt=TICK_DT(0.05)` 로 썼는데, 그 tick 의 확장 예산이 `int(0.31)=0` 이라 루프가
  **한 번도 돌지 않았다.** 그래서 `appendleft` 를 `append` 로 바꾸는 변이를 넣어도
  17개 테스트가 전부 통과했다. 예산·누적이 걸린 로직을 잴 때는 **먼저 예산이 1 이상인지
  단언**할 것. (통과는 증거가 아니다 — 실제로 이 한 건이 그랬다.)
- **측정 코드가 `paused` 를 관찰 창으로 쓰면 안 된다.** AI 가 같은 tick 에 고르고
  정지가 풀리므로 호출자 눈에는 `paused` 가 사실상 항상 False 다. 처음 3판 측정에서
  최저 비용 배율이 `1.000` 으로 나왔던 이유다 — 주기 샘플링으로 바꿨다.
- **탈락·승리 판정이 헤드리스 테스트를 망친다.** 1인 지도에서 부대를 굴리면 첫 tick 에
  정복 승리가 나서 `tick()` 이 곧바로 반환하고 부대가 영원히 진행하지 않는다.
  공격을 재는 테스트에는 반드시 **더미 상대**를 하나 둘 것.

### 실측 — 240판 4인전 기준선 (seed 1000~1239)

| | |
|---|---|
| 시간 종료 | 123판 (51.2%) |
| 지배 | 117판 (48.8%) |
| 판 길이 중앙 | 900초 (최단 55) |
| 승자 점유 중앙 | 77.7% |
| 증강 정지 중앙 | 7회 (설계값과 일치) |
| 비용 하한(0.2) 도달 | 22판 (9.2%) |

증강 승자비중(4인전이므로 기대 25%):

| 강함 | | 약함 | |
|---|---|---|---|
| 개척단 | 37.4% | 강행군 | 21.0% |
| 산악병 | 33.8% | 초토화 | 21.3% |
| 삼림 순찰대 | 26.2% | 징집령 | 21.5% |

**읽는 법:** 이긴 카드가 전부 *중립 확장을 싸게 하는* 카드고, 진 카드가 전부
*싸움에 쓰는* 카드다. 51%가 시간 종료라는 것과 같은 말이다 — 판이 "서로 못 뚫고
중립만 먹다가 시간이 다하는" 모양이다. 5절의 옛 교착(29/40 시간 종료)과 증상이 같다.

⚠ 승자비중은 카드 **레벨 수**로 세었다. 같은 판 안의 레벨은 독립 시행이 아니므로
표준오차가 이항분포 계산(±1.8%p)보다 크다. 개척단(+12.4%p)은 확실하고, 26% 대는
노이즈일 수 있다.

---

## 6.5 교착 추적 (2026-08-21 오후)

51% 시간 종료의 원인을 규칙 / AI 중 어디인지 몰라서, AI 튜닝 상수를 CLI 로 덮어쓰게
하고 12조합 × 100판을 **같은 seed** 로 돌렸다(지도 변량을 공유해 짝지어 비교).

### 결론: AI 가 주범이었다. 다만 **상수 하나로는 안 움직인다**

| bias | conc | fill | 시간 종료 |
|---|---|---|---|
| 1.6 | 1 | 0.55 | 53.0% (기준) |
| 1.0 | 1 | 0.55 | 53.0% ← 모든 자릿수가 동일 |
| 0.4 | 1 | 0.55 | 53.0% |
| 1.6 | 2 | 0.55 | 53.0% |
| **0.4** | **2** | 0.55 | **39.0%** |
| **0.4** | **2** | **0.35** | **33.0%** |

`NEUTRAL_BIAS` 만 내리거나 `MAX_CONCURRENT` 만 올리면 **한 자릿수도 안 움직인다.**
둘이 같이 있어야 효과가 난다. 폴링 중간에 앞 6줄만 보고 "AI 원인 기각" 이라고 적었다가
마지막 줄에서 뒤집혔다 — **스윕은 다 돌기 전에 읽지 말 것.**

### 채택 (240판, seed 1000~1239)

| | 옛 AI | 새 AI |
|---|---|---|
| 시간 종료 | 51.2% | **41.7%** |
| 판 길이 중앙 | 900초 | **619초** |
| 승자 점유 중앙 | 77.7% | 80.0% |
| 카드 승자비중 폭 | 21.0~37.4% | **22.4~29.2%** |
| 비용 하한 도달 | 9.2% | 11.3% |

100판 스크리닝은 33% 였는데 240판에서 41.7% 였다. **40~100판은 방향까지만이다.**

### 함께 무너진 가설 두 개

- **"지도가 지배를 막는다"** — 최대 대륙이 육지 80% 미만인 지도는 100장 중 6장뿐.
  기각.
- **"도달 불가 섬이 육지의 9% 를 영구히 묶는다"** — 옛 AI 에서는 맞았지만(남은 중립
  10.2%), 새 AI 에서 **2.1%** 로 떨어졌다. 지도가 아니라 **AI 가 못 먹고 있던 것**이다.

### ⚠ 남은 구조적 결함 — 충전율이 포화한다

후반(300초~) 충전율이 **영토 규모와 무관하게 93% 확률로 1.0 에 박힌다.**

| 영토 | 1.0 에 박힌 비율 |
|---|---|
| <50칸 | 96.8% |
| <150칸 | 90.5% |
| <400칸 | 87.6% |
| 400칸+ | 85.9% |

성장률이 영토에 비례하는데 공격은 병력의 25% 만 쓰므로, 소모가 회복을 절대 못 따라간다.
결과로 **방어 계수가 모두에게 3.2배 상수**가 되고, 충전율이 후반에 아무것도 구분하지
못한다. 5절이 "타일당 병력(절대값) → 충전율" 로 고쳤다고 적어 뒀지만 **충전율도
포화한다** — 같은 함정의 다음 층이다.

이게 AI 까지 망가뜨린다: `simple_ai.choose_target` 의 "약한 쪽을 먼저 친다" 가 모든
상대 점수를 0.333 으로 만들어 죽은 코드가 되어 있다.

---

## 7. 재개 지점은 **여기가 아니다**

> ⚠ **2026-09-04 정리.** 이 절이 `docs/implementation-log.md` §7 과 **경쟁하는 두 번째
> 재개 지점**이었다. `CLAUDE.md` 5번이 경고한 그 모양이다 — *"살아 있는 계획은 한
> 자리에만."*

**재개 지점은 `docs/implementation-log.md` §7 하나뿐이다.** 여기에는 다시 적지 않는다.

옛 본문이 가리키던 것 넷(`DEFAULT_ATTACK_RATIO` · `TROOPS_GROWTH_RATE` ·
`NEUTRAL_BIAS` · `MAX_CONCURRENT`)은 **코드에 하나도 없다.** 1번 항목
*"충전율 포화를 푼다"* 는 그 규칙이 구현으로 사라져 **문제 자체가 없어졌고**,
3번 *"UI 를 만든다"* 는 끝났다. 2번 *"할인 중첩을 다시 잰다"* 는 §3 에서
**측정해서 닫았다**(적어 둔 셋이 다 틀렸다).

<details>
<summary>옛 본문 (2026-09-04 에 넷 다 없는 이름으로 확인)</summary>

1. **충전율 포화를 푼다** (6.5절). 후보: 공격 비율을 올린다(`DEFAULT_ATTACK_RATIO`
   0.25) · 성장률을 낮춘다(`TROOPS_GROWTH_RATE` 0.085) · 방어 계수를 충전율이 아닌
   다른 축으로
2. 그 뒤 **할인 중첩(3절)을 다시 잰다.**
3. UI (`ui/`) — 5절 그래픽 함정을 먼저 읽을 것.

밸런스 측정은 판당 노이즈가 크므로 **40판은 방향, 채택 판단은 240판**으로 본다.
240판이 약 75초다.

</details>

⚠ 마지막 줄(*"240판이 약 75초"*)만은 **지금도 유효한 교훈의 형태**다: 판당 노이즈가
크므로 적은 판으로는 방향까지만 본다. 다만 **숫자는 완전히 다르다** — 지금 판은
하나가 33~37분이라 240판을 돌릴 수 없고, 그래서 §5.116 의 증강 A/B 도
**12짝**으로 재고 *"생존율 1판 차이로는 아무 말도 못 한다"* 고 적었다.
